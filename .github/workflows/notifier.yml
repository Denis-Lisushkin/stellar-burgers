name: PR Telegram Notifier

on:
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram Notification
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # Функция для экранирования MarkdownV2 спецсимволов
          escape_md2() {
            echo "$1" | sed -E 's/([_\*\[\]\(\)~`>#+\-=|{}.!])/\\\1/g'
          }

          # Экранируем переменные
          HEAD=$(escape_md2 "${{ github.event.pull_request.head.ref }}")
          BASE=$(escape_md2 "${{ github.event.pull_request.base.ref }}")
          TITLE=$(escape_md2 "${{ github.event.pull_request.title }}")
          ACTOR=$(escape_md2 "${{ github.actor }}")
          URL=$(escape_md2 "${{ github.event.pull_request.html_url }}")
          ACTION="${{ github.event.action }}"
          MERGED="${{ github.event.pull_request.merged }}"

          # Формируем многострочное сообщение через EOF
          if [ "$ACTION" = "opened" ]; then
            MESSAGE=$(cat <<EOF 📢 *Новый Pull Request!* 👤 Автор: $ACTOR 🔀 Из ветки: $HEAD ➡️ В ветку: $BASE 📝 Заголовок: $TITLE 🔗 $URL EOF)
          elif [ "$ACTION" = "closed" ] && [ "$MERGED" = "true" ]; then
            MESSAGE=$(cat <<EOF ✅ *Pull Request смержен!* 👤 Автор: $ACTOR 🔀 Из ветки: $HEAD ➡️ В ветку: $BASE 📝 Заголовок: $TITLE 🔗 $URL EOF)
            
            # Напоминание про git pull для ветки dev
            if [ "$BASE" = "dev" ]; then
              MESSAGE="$MESSAGE ⚠️ *ВАЖНО! Не забудьте выполнить:* \`git pull origin dev\`"
            fi
          else
            exit 0
          fi

          # Отправка сообщения в Telegram
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d parse_mode="MarkdownV2" \
            -d text="$MESSAGE"
