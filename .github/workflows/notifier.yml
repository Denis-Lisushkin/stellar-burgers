name: PR Telegram Notifier

on:
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram Notification
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # Функция для экранирования MarkdownV2 спецсимволов
          escape_md2() {
            echo "$1" | sed -E 's/([_\*\[\]\(\)~`>#+\-=|{}.!])/\\\1/g'
          }

          # Получаем данные PR
          HEAD="${{ github.event.pull_request.head.ref }}"
          BASE="${{ github.event.pull_request.base.ref }}"
          TITLE="${{ github.event.pull_request.title }}"
          ACTOR="${{ github.actor }}"
          URL="${{ github.event.pull_request.html_url }}"
          ACTION="${{ github.event.action }}"
          MERGED="${{ github.event.pull_request.merged }}"

          # Формируем сообщение с эмодзи
          if [ "$ACTION" = "opened" ]; then
            MESSAGE="📢 *Новый Pull Request!* 👤 Автор: $ACTOR 🔀 Из ветки: $HEAD ➡️ В ветку: $BASE 📝 Заголовок: $TITLE 🔗 $URL"

          elif [ "$ACTION" = "closed" ] && [ "$MERGED" = "true" ]; then
            MESSAGE="✅ *Pull Request смержен!* 👤 Автор: $ACTOR 🔀 Из ветки: $HEAD ➡️ В ветку: $BASE 📝 Заголовок: $TITLE 🔗 $URL"

            # Напоминание про git pull для ветки dev
            if [ "$BASE" = "dev" ]; then
              MESSAGE="$MESSAGE ⚠️ *ВАЖНО! Не забудьте выполнить:* \`git pull origin dev\`"
            fi
          else
            exit 0
          fi

          # Экранируем весь текст перед отправкой в Telegram
          MESSAGE_ESCAPED=$(escape_md2 "$MESSAGE")

          # Отправка сообщения в Telegram
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d parse_mode="MarkdownV2" \
            -d text="$MESSAGE_ESCAPED"
